import { execSync } from "node:child_process";

export const createmicrofrontend = ({ type }) => {
  execSync(`npm install @brojs/frontend-init`, { stdio: "inherit" });
  execSync(`npx brojs-f-init ${type}`, { stdio: "inherit" });
};
