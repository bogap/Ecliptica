import { execSync } from "node:child_process";

export const createIjlCli = (advanced) => {
  execSync(
    `npm install @ijl/cli && npx ijl-cli init ${
      advanced === "advanced" ? "--advanced" : ""
    }`,
    { stdio: "inherit" }
  );
};
